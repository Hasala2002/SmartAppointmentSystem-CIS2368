import { Appointment } from '../../types';
interface AppointmentCardProps {
    appointment: Appointment;
}
export declare const AppointmentCard: ({ appointment }: AppointmentCardProps) => import("react/jsx-runtime").JSX.Element;
export {};
